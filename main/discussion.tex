\section{Discussion}
\label{sec:discussion}

The central finding is not that more subjects are unimportant, but that a
standard fixed-total experiment does not estimate their isolated value. Across
three datasets, the label-agnostic design produces an apparently general
subject-breadth advantage. Freezing the marginal training label distribution
removes that conclusion: corrected effects are small, model- and
cohort-dependent, and unresolved once training-subset and test-subject
uncertainty are propagated jointly. Patient-disjoint splitting is necessary,
but it is not sufficient for interpreting subject--cell scaling.

The class-matched protocol deliberately targets a narrower estimand. It holds
observed labels, per-class cell counts, and class proportions fixed while
changing how many subjects contribute those cells. It does not fix
disease--subtype composition, within-class subject heterogeneity, or unrecorded
pool and batch structure. The remaining effect is therefore still
associational, and a near-zero estimate under the tested budgets is not a
universal saturation law. Nevertheless, the protocol identifies the source of
an otherwise misleading empirical regularity and provides a reproducible way
to test it in future multi-subject single-cell cohorts.

The APT case studies clarify the scope rather than add parallel stories.
Hierarchy prediction demonstrates that leakage and granularity alter the
apparent learning problem, while the nested patient audit shows that correct
splitting cannot resolve unmeasured acquisition confounding. Neither a complex
hierarchy model nor a compact feature panel repairs an ill-specified estimand.
For clinical single-cell evaluation, subject and cell budgets, label support,
split units, and both uncertainty sources should therefore be reported as part
of the benchmark contract.

\section{Conclusion}
\label{sec:conclusion}

Fixed cell totals can confound subject breadth with the training label
distribution. A class-matched, jointly resampled evaluation reverses or removes
the apparent subject advantage across APT, COMBAT, and OneK1K, while APT
hierarchy and patient-characterization case studies expose the practical
consequences of statistical-unit and nuisance mismatches. APT-Bench therefore
contributes an evaluation protocol for asking subject--cell scaling questions,
not a universal scaling law or a claim of model superiority.
